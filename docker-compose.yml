services:
  # Landing Page - furrycore.com.br
  landing:
    build:
      context: .
      dockerfile: ./apps/landing/Dockerfile
    container_name: furry_landing
    restart: always
    expose:
      - "20000"
    env_file: .env
    environment:
      - PORT=20000
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:20000')"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - furry_net

  # Rede Social - pawsteps.social
  pawsteps:
    build:
      context: .
      dockerfile: ./apps/pawsteps/Dockerfile
    container_name: furry_pawsteps
    restart: always
    expose:
      - "20001"
    env_file: .env
    volumes:
      - ./apps/pawsteps/database:/app/database
      - ./apps/pawsteps/static/uploads:/app/static/uploads
      - ./apps/cgrf/database:/app/shared_data/cgrf
      - ./apps/shop/database:/app/shared_data/shop
    environment:
      - PORT=20001
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:20001')"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - furry_net

  # Portal CGRF - cgrf.com.br
  cgrf:
    build:
      context: .
      dockerfile: ./apps/cgrf/Dockerfile
    container_name: furry_cgrf
    restart: always
    expose:
      - "20002"
    env_file: .env
    volumes:
      - ./apps/cgrf/database:/app/database
      - ./apps/pawsteps/database:/app/shared_data/pawsteps
      - ./apps/shop/database:/app/shared_data/shop
      - ./shared:/app/shared
    environment:
      - PORT=20002
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:20002')"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - furry_net

  # Loja - furrycore.com.br/shop
  shop:
    build:
      context: .
      dockerfile: ./apps/shop/Dockerfile
    container_name: furry_shop
    restart: always
    expose:
      - "20003"
    env_file: .env
    volumes:
      - ./apps/shop/database:/app/database
      - ./apps/cgrf/database:/app/shared_data/cgrf
    environment:
      - PORT=20003
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:20003/')"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - furry_net

  # Proxy Reverso (Nginx)
  nginx:
    image: nginx:alpine
    container_name: furry_proxy
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./certs:/etc/nginx/certs:ro
      - ./nginx_logs:/var/log/nginx
    depends_on:
      - landing
      - pawsteps
      - cgrf
      - shop
    networks:
      - furry_net

  # Zabbix Agent para Monitoramento
  zabbix-agent:
    image: zabbix/zabbix-agent2:latest
    container_name: furry_zabbix_agent
    restart: always
    ports:
      - "10055:10050"
    environment:
      - ZBX_SERVER_HOST=187.77.233.230
      - ZBX_HOSTNAME=SRV-AUTOMACAO
    networks:
      - furry_net

  # Admin Dashboard - Central de Controle
  admin-dash:
    build:
      context: .
      dockerfile: ./apps/admin-dash/Dockerfile
    container_name: furry_admin
    restart: always
    ports:
      - "29999:29999"
    volumes:
      - ./apps/admin-dash/database:/app/database
      - ./nginx_logs:/app/shared_logs
      - ./apps/cgrf/database:/app/shared_data/cgrf
      - ./apps/pawsteps/database:/app/shared_data/pawsteps
      - ./apps/shop/database:/app/shared_data/shop
    env_file: .env
    networks:
      - furry_net
    depends_on:
      - nginx

networks:
  furry_net:
    driver: bridge
